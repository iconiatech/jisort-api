export enum MenuActionType {
  TICKET = 'ticket',
  ANSWER = 'answer',
  SUBMENUS = 'subMenus',
  PRODUCTS = 'products',
}

export enum MenuResponseType {
  NUMBER = 'number',
  STRING = 'string',
  NONE = 'none',
}
